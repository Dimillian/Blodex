import type { GameConfig } from "./types";

export const GAME_CONFIG: GameConfig = {
  tileWidth: 96,
  tileHeight: 48,
  gridWidth: 64,
  gridHeight: 64,
  floorClearKillTarget: 12,
  enemyBaseHealth: 85,
  enemyBaseDamage: 7
};
